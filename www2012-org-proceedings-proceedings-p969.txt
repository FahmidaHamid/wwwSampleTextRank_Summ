 to execute a service, all inputs have to be provided by other services or the user. the authors of [3] propose that providers register their services at service communities. the set of selected services is called service selection. qos-aware service selection. although they consider the worst-case scenarios of qos, they do not consider service failures. qos models. each service is represented by a node in the graph. if a service fails, the services from the ordering are used to replace the service.