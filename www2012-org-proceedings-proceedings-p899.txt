 what are desired properties of a metro map? (metro map). consider, for example, chain a. now, consider chain b. at each iteration, we expand the chain which features the highest coherence, generating all of its extensions. example 1 (map). we then hypothesize a model for the rest of the chain (line 4). map summarize the topic of the task? in other words, map users avoided some storylines intentionally. the cloud describes the documents of the map.