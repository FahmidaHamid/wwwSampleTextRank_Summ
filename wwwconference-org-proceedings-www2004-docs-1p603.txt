 we identify constraints that must be ful lled by the wsdl description and the xquery module that it is bound to. xquery modules xquery adopted a notion of module in its may 2003 working draft.   a porttype which regroups all the operations supported by a service. connecting xquery modules to wsdl there is a natural parallel between xquery modules and wsdl descriptions. after) calling the service. we will see more in section 5 about the relationship between xquery modules and wsdl descriptions. based upon that wsdl  le, an xquery soap server is this is the direction used when importing a service in xquery (see section 3). sequence types in xquery do not provide all the functionalities required to describe parameters from operations in wsdl.