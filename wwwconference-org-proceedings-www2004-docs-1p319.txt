 they are correlated when a user submits queries, a user browses web pages, and a query references web pages. for example, web search [4, 17] uses the web page space and hyperlinks within the space; collaborate filtering [14] uses the document (web page) space, the user space, and the browsing relationship in-between; web query clustering [22] uses the web page space, query space, and reference relationship in-between. we apply the link fusion algorithm from eq. to evaluate the link fusion algorithm. http://www.vindis-cambridge.audi.co.uk). the link fusion returned by algorithm table 3.