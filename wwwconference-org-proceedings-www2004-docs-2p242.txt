  is transformed into a hypermedia presentation in a format suited to a user. the presentation layer defines the presentation layout that together with the definitions from the application layer is needed for the generation of a presentation for a concrete platform. there are two types of slice properties: slice composition (a slice encloses another slice) and slice navigation (a slice is the anchor of a hyperlink to another slice). to user preferences. allowing the user to submit forms.