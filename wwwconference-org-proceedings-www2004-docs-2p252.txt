 we assume that the user ontology represents the user s knowledge, and we use the user ontology as an user pro le to achieve a user adaptation on a3. in sharing a user ontology, each user must have one user ontology, not many ontologies. there, the user ontology is used for achieving a user adaptation and is reconstructed based on the interaction between the user and the site/application. to solve these problems, the user ontology manager performs all actions on the user ontology. the user ontology consists of category and resources.