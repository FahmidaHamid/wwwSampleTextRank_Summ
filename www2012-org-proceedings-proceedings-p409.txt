 we refer to such patterns as query templates and their constituents, such as brand and model, as attributes. we refer to such patterns as query templates and their constituents as attributes. when constructing a query a user picks a template, and then picks words from the associated attribute. here, each attribute has its own word distribution from which the words are chosen to instantiate the query. the procedure ends with an assignment of each query to a template, and each word in the query to an attribute. these attributes can then be used to construct the query templates. the problem of assigning an attribute to each word in a query has been explored in [3, 25]. query logs.