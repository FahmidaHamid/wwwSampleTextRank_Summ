 in an xforms page, form data is represented as xml. what data are sent, where the data are sent, and how the data are sent is all specified in the xforms markup. hx. at runtime, these artifacts configure struts to manage the transitions between application pages. a page node represents an xforms page, which executes on the client. the application contains two pages   input.hx and output.hx. the application can be deployed on any web server that supports j2ee web applications, version 2.3 or 2.4.