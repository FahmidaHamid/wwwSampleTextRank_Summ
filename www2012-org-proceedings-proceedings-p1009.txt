 query, asking   tag query, asking users to provide either global tags or tags about specific attributes of c. e is the crowd engine or engines to be used in the query.   h represents constraints in the execution of a query q; in particular, it indicates which conditions should hold for a query to be terminated. based on these choices and on the input model, the query is possibly split and routed to groups of users, and then query the master user then defines a crowd query through the query template, which is integrated with the seco exploratory interface. at the selected time, crowdsearcher collects received results and summarizes them in the query result). in case (a), the query is posted on the facebook s wall of the master user, and friends are invited to raise the counts of likes.