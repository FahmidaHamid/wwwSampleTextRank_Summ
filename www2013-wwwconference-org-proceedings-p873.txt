 an alternate approach to recreate the user pro le could be via mining the inputs to the search engine (i.e. the user s search query logs, results clicked, browsing history etc. topic block the topic block follows the description of standard topic models (c.f.      personalization block we retrieved on average 850 distinct queries for each user.  ltp but do not reveal the user it belongs to. user id for this experiments, we extracted   values over a common set of topics for each user. this shows that   is in fact learned tailored to the personalization of each user. ), which a user may not be comfortable with.