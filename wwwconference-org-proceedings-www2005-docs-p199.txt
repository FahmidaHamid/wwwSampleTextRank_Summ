 this system provides a graph-based interface to any rdf repository. domain-dependent rdf structure search. rdf structure is a node-edge graph. however, the user can also navigate the document structure systems such as noadster creates around these returns. in a given domain, and for a given user, some concepts are more important than others for generating document structure. having described how to get a document structure of relevant links from an rdf repository, we now describe how to display each link when selected. in rdf, properties values that are not literals are uris.