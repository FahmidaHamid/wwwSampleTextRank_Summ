 in order to generate an image of the web page, this module displays the obtained data as a web page in a virtual web page viewer. this function helps users to know the contents of each web page. the system extracts the urls included on a web page, and then accesses each url to generate images of web pages. for example, an user can not see the design of a linked web page. our 3d web bookmark system can offer an alternative user interface for web bookmarks.