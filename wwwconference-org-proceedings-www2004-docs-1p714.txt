 ontologies are used to represent concepts and relationships of data on the web and they are expressed in terms of ontology languages. we used this tool to transform the military plan ontology into z section military, with daml2zrules as parent. any such error found by z/eves will be corrected in the original ontology. as introduced in section 4, the military plan ontology de nes concepts in the military domain. we then load the ontology into oiled and classify it, as described in step 2 of section 5.1. since this is not an instance ontology, the 4th step is not performed. the instance ontology plana.daml contains altogether 954 statements, describing 195 resources (rdf subjects). using z/eves, we discovered 14 errors in one instance ontology. web ontology language (owl) use cases and requirements.