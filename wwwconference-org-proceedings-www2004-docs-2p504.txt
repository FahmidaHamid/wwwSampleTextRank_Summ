 moreover, we propose to further optimize resource usages by integrating data multicast into service multicast.  on tree node  functional service tree ps (a) ps (b) between service tree node service branch (for an using a unicast service routing solution. it is easy to see that service multicast de nitely helps to save proxy machine resources because each service in the functional service tree gets executed only once.