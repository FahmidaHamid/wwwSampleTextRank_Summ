 learning objects can then be reused across courses, disciplines, and institutions. each lesson title is a hyperlink to launch the course in the course player starting at the given learning object. object. these types represent potential roles for the learning object in future assemblies. custom courses are usually between 1 and 10 learning objects with each learning object taking 5 to 20 minutes. java objects are created for the learning object metadata as needed by the metadata manager. the lom provides a relations category, but learning object metadata relations in lom can only be between individual learning objects.