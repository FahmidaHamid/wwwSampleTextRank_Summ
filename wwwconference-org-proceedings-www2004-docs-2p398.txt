 the web content includes thumbnails of the video units and captioning data generated from the metadata. the length of the video units are represented by the sizes of the thumbnails. intuitively, larger thumbnails have longer video units. we describe a method of generating web content from a video stream with metadata. that is because a thumbnail has several video units, which are constructed from several scenes. if they zoom out, the video will be divided into two smaller thumbnails (b).