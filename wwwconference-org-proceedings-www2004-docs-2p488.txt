 the ontology uml profile - oup) into the forthcoming web ontology language (owl) [1]. the ontology uml profile class-oriented stereotypes (an excerpt of the wine ontology)  the main idea of having an uml profile for ontology development is to use present uml tools. on the other hand, since we obtain an owl described document, we do not need to use any ontology tool, instead we are able to use this ontology description as a final owl ontology.   uml tools can only draw uml models, but they do not have an ability to check the completeness of an oup ontology. then we exported this extended uml into xmi, and after performing the xslt, we obtained an owl document. these two ontologies are: the ontology of saints and philosophers, and the petri net ontology.