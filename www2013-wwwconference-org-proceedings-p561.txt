 we will introduce the function  type to denote the function that maps query events to one or more types of entity referenced in the query. a navigational query is a query entered with the intention of navigating to a particular website [9]. we chose option (2) above, in which we compare manually created < query, entity > and < entity, type > pairs to automatically created links. for task 1, the highest accuracy and auc were obtained using all the query features, that is, using the query, its entity and type but not the loosing site.