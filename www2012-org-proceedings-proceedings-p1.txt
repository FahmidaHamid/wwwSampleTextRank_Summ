 music users. music users. we model playlists using a latent factor model, where each playlist induces a probability distribution over the items. model parame- henceforth, we denote all model parameters by  . in the following we will call such a model a station-less model. this represents a scenario of exploiting an existing model for predicting new stations or users. music users. we use the model to rank the two items for the relevant station (or user). item (i.e., the item with more plays overall). we compared our model against three baselines, as follows:   popularity. music #playlists music data.