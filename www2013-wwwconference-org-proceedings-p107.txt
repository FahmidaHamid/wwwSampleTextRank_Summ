 any current web page can be thought of as an html mockup just one that happens to be published. using cts, they can then re-theme their web page as any other page that uses the same set of selectors. what if this professor could not modify his html? the web components spec provides an html syntax for declaring and reusing web widgets. web components also commingle widget definitions and mapping rules with the html document.