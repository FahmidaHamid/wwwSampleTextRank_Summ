 normally, these methods display the whole web page. we assume that the user is entering a web page from a link. the web is treated as a graph on which surfers move randomly from page to page according to the links on the page. to construct a graph from a web page, we first identify the nodes, which are the basic elements in the web page. each of the links will be treated as basic element. another web page may have a text paragraph with a link. content and the original web page. we try to deliver all the information that a user wants in a web page. we achieve this by ranking the importance of each element in a given web page and generating a customized  web  for mobile devices.