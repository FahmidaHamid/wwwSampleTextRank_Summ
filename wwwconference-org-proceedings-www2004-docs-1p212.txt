 imagine the interaction between a user and a website to be a conversation between two participants. such an interaction where the user and the site exchange initiative is called a mixed-initiative dialog. let us investigate how to specify a browsing interaction with the politicians website. transformation rules. the interaction manager primarily coordinates communication between the transformation engine and the interaction interfaces. notice that intermediate dialog states might not correspond to any of the site s existing webpages (especially after some out-of-turn interaction), so the interaction manager must mediate the dialog to the fullest. user experiences with out-of-turn interaction are described in [33];