  component html browser xml application / xml browser xml browsers can realize the semantics of the xml data through the related xml application. components are building blocks for constructing xml applications.  xml application xml application communication services in the xvm a component implements one or more interfaces. the xvm dynamically loads  interpreters  and uses them to  interpret  xml  instructions . the xml browser is using wcml [7], an application of xml.