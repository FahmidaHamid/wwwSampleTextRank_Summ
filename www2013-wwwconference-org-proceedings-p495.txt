 all the above methods can be used to generate recommendations for an individual user. we now describe how the model above can be augmented in order to provide group recommendations. the relevance of an item to any given group will vary with the type of group that seeks recommendations. we denote the rating that user u gives item i as r(u, i).  in computing the ndcg score for each user, we assign a  0  rating to all the items that were not rated by the user, so that if the model recommends any item that was not rated by the user it will be penalised. items) as this user, and other items which have the same features (i.e. users) as this item.  table 3: group recommendation.