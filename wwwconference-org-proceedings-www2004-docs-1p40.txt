 existing type system. we have implemented webssari (web application security by static analysis and runtime inspection) as a framework for extending existing script languages with our system. security violation figure 6. a model of common web application vulnerabilities. here we have tried to design an approach that retains the advantages of both while also eliminating their disadvantages. type qualifiers are also used as a means for developers variables.  a type environment variables to security classes at a particular program point.  x type  ( ) ) security class. compared to waves, webssari offers a sound verification of web application code.