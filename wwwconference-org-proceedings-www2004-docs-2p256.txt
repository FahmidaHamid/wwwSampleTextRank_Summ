 wedit enables users to edit text using a web browser. the additional information in this paper means annotations and comments for texts on web page. wedit enables users to replace, cut, and add plain text and html tags. all wedit processes can be carried out using an existing web browser. one text is a normal html text, which does not include extra tags and scripts, and another is a text for event handling which includes javascript (event handling text). in the above script, the selected text and the web page url is sent to cgi scripts for text processing. the user edits the text and submits the edited text (in this example, the text  attention! when a user wants to add additional information to a web page, they can add the information using the wedit mechanism. the additional information added to the web page is stored in the edit database which is maintained by the proxy agent.