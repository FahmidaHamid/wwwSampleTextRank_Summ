 both tasks can again be supported by using an ontology. here again, interaction with the user goes along the ontology. then we describe how it can be used to interact with the ontology. in both cases, the ontology browsing component is used to de ne the elements that should be looked for. the user can specify his preferences by assigning weights to selected entities of the ontology.