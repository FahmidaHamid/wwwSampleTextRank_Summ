 if the template language is too powerful, template designers risk entangling template and business logic. a context-free template is a template whose actions are limited to referencing attributes, ai and templates. a template is a list of literals, attribute references, and template references. actions are attribute references and template references. attributes are  pushed  into a template like a stream of tokens. a template engine s entanglement index is the number of separation rules that a template may violate. each template may have its own renderer. templates can be applied to the results of other template applications.