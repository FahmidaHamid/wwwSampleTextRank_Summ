 we analyzed each application and reproduced the reported csrf attacks. unlike cookies or http  request. if they do not match, the request is con- once the browser obtains an arl policy for a site, the browser must examine each outgoing http request s context to check whether any of a site s current arl policies apply, and if so, whether or not to attach arled credentials to the request. some web sites mix http and https content when presenting web pages to users. using these traces, we then developed an arl policy for each site.